vector<int> rotLeft(vector<int> a, int d) {
    vector<int> result;
    for (int j = d; j < a.size(); ++j){
        result.push_back(a[j]);
    }
    for (int i = 0; i < d; ++i){
        result.push_back(a[i]);
    }

    return result;
}
